# 04 — from_city 哨兵归一化（无→北京 一致性）

**What to build:** 提取 LLM 可能把「没提城市」输出成「无」（prompt 要求「待定」但 LLM 有方差）。
「无」不在现有未知哨兵集里 → 绕过缺项检查、绕过常驻城市补全，带着 from_city="无" 去生成，
规划 LLM 默认当北京（无→北京幻觉），但记忆记「无」→ 历史显示「无→杭州」与「待定→杭州」并存，不一致。
修法：提取后统一归一化「无/未知/出差」→「待定」，让缺项检查、常驻补全、记忆、历史看到同一哨兵。

**Blocked by:** None

**Status:** ready-for-agent

- [ ] `trip_planner.py`：提取后归一化 from_city/to_city 的「无/未知/出差」→「待定」；`_UNKNOWN` 提为模块级常量并含「无」（两处共用：缺项检查 + 常驻补全）
- [ ] 单测（RED→GREEN）：`_missing` 对「无」报缺项；plan() 提取出「无」+ 有常驻城市 → 补全后生成；无常驻城市 → 缺项短路（不再静默进生成）
- [ ] 门禁全绿 + 黄金集基线无回退

## Answer

已实现并验证：_UNKNOWN_CITIES 模块级常量（含「无」）+ plan() 提取后归一化；
4 新单测（缺项报错/常驻补全/缺项短路/to_city 对称）全绿，门禁全绿，黄金集 99% 无回退。
