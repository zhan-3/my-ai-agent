# 晓问 · 差旅助手

企业差旅多 Agent 助手：主管通过有界 Agent Loop 调用**注册表动态发现的子 Agent**（内置六个 + 外部扩展），配套两层记忆（短期对话 + 长期偏好/历史）与知识检索、联网查询能力。

## 语言

**晓问**:
产品名（企业差旅助手）。只服务企业差旅；个人休闲/非差旅一律归「其他」。
_Avoid_: 我的 agent、助手（泛指时用）

**主管**:
面向用户持续完成目标的主 Agent。生产实现采用有界 Loop：读取 transcript 与运行状态，决定调用哪个子 Agent，观察结果后继续决定、追问或结束。
_Avoid_: 固定意图分类器、预定义路由图

**Agent Loop**:
主管单次运行内的 `decide → 调用子 Agent → observe → decide → final` 循环。循环持久化 assistant/tool-call/tool-result 轨迹，并受步骤、时间、token 与重复调用限制。它只改变主管控制流，注册中心与子 Agent 继续承担领域执行。
_Avoid_: 固定条件边、装饰性 while、无限自治

**子 Agent**:
主管之下执行具体任务的实体模块，声明 INTENT / DESCRIPTION / run(state)（统一接口）。内置六个：行程规划、偏好记录、历史查询、知识问答、联网查询、其他；外部扩展（plugins/）动态并入（如 差旅统计）。由注册中心自动扫描注册、懒加载、渐进式披露。
_Avoid_: 处理器、handler、worker（旧称）

**注册中心**:
plugin_registry：discover() 扫描内置 agents/ + 外部 plugins/ 返回 manifest（AST 只读元数据，零加载）；load_agent() 派发时才加载；内置优先。
_Avoid_: 插件目录（仅指外部扩展时用）

**意图**:
主管对用户请求的分类，词汇表来自注册表 manifest（六内置 + 外部扩展），不在清单内或与业务无关归「其他」。
_Avoid_: 类型、分类

**子任务**:
一句话里多个独立请求时拆分出的单元（各带自己的意图与原文），由调度优化并行派发；单一请求时为空。
_Avoid_: 子请求、分支

**会话**:
一轮完整交互的闭环——读取线程 transcript 与活跃任务 → 运行主管 Agent Loop → 持久化工具轨迹与任务变化 → 写回用户与助手消息。
_Avoid_: 聊天、交互（泛指时用）

**对话线程**:
前端可见的一段连续对话，由 `conversation_id` 标识；Web 层将其与 JWT 用户组合为 `thread_id`。短期记忆、活跃任务和轮次锁按线程隔离。
_Avoid_: 用户（一个用户可有多个线程）

**活跃任务**:
线程内尚未完成的行程（trips 表中 `drafting` 态）：含聚焦续接文本与缺失差旅要素。偏好、政策或实时查询可以临时打断它，但不会覆盖；补齐要素后转为待出发，明确取消后转为已取消。一个线程可同时有多条 drafting，主管续接时全部注入、由模型判断续哪条。
_Avoid_: 最近六轮文本（文本不是任务状态）；单活跃任务指针（已被集合式续接取代）

**短期记忆**:
最近 N 轮对话消息。每轮 invoke 前注入（克制截断）、invoke 后写回用户与助手两轮。对应 LangChain 的 checkpointer+thread 概念。
_Avoid_: 上下文、最近对话（口语用）

**会话隔离 / 用户隔离**:
短期 transcript 与活跃任务按 `thread_id` 隔离；长期偏好和历史行程按 JWT 解出的 `user_id` 隔离。客户端只能提交 `conversation_id`，不能指定用户。内部 `session.chat()` 省略 `user_id` 时兼容旧调用，将 `session_id` 同时用于两个维度。存储唯一后端为 Postgres，`POSTGRES_URL` 必配（ADR-0006/0007/0009）。
_Avoid_: 多角色（授权未做，用户维度 ≠ 角色维度）

**长期记忆**:
跨会话持久信息，两类：偏好（含常驻城市，支持追加/覆盖）与历史行程。对应 LangChain 的 store 概念。

**常驻城市**:
长期记忆中的用户常驻城市。行程规划时用户未给出发城市则用它补全。
_Avoid_: 出发城市（缺省值，不是同一概念）

**行程**:
一次出差计划。行程子 Agent 两阶段生成（要素提取→行程生成），写回长期记忆供历史查询与续接。行程有稳定身份（trip id）与生命周期四态：规划中（drafting）/ 待出发（upcoming）/ 已完成（completed）/ 已取消（cancelled）；「进行中（出差期间）」不是独立状态，而是待出发行程按日期派生的展示标签。
_Avoid_: 计划、方案

**行程呈现策略**:
短差（≤5 天）逐日列要点；长差（>5 天）只列首日/用户明说的安排/末日，空白停留日折叠为一行。餐饮偏好不逐日重复：只在行程里提示一次（🍽️ 饮食偏好行），报销标准放在预算块。

**差旅要素**:
行程规划所需的输入事实：出发/目的城市、出发日期、天数、住宿偏好、预算偏好。要素不全时不硬生成，先向用户索取。
_Avoid_: 参数、字段

**缺项提示**:
行程子 Agent 发现必填要素缺失时返回的向用户索要清单，而非硬生成行程。
_Avoid_: 报错、校验失败

**偏好**:
用户对差旅的个人倾向（住宿/餐饮/交通/预算/常驻城市/其他）。支持追加与覆盖（同类别更新时替换旧条目）。
_Avoid_: 设置、配置

**行程状态**:
行程生命周期的四个存储状态：`drafting`（规划中，要素不全）、`upcoming`（待出发，已确定未结束）、`completed`（已完成，返程日/最后一天已过，只读）、`cancelled`（已取消，终态）。completed 默认读时按日期派生（无需后台任务），另支持手动标记；改期/改细节更新同一条行程，参考历史再来一次复制新行程。
_Avoid_: 三态时空分类（历史/进行中/已规划——那是差旅画像的读时分桶，非存储状态）
